# Requires policy/gcp/mitre/helpers.rego to be deployed alongside this policy.
package policy_name

import data.mitre_helpers as helpers
import rego.v1

violation contains {"msg": "violation message goes here", "details": details} if {
	# add relevant permission or method glob patterns here
	# ("." delimited, matched case-insensitively against both the granted
	# permission and the request method name)
	patterns := [
		# "**.disks.createSnapshot",
	]

	some auth in helpers.matched_entries(patterns)
	details := helpers.details(auth)
}
